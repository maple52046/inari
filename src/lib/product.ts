/**
 * Public identity of the product.
 *
 * Shared so the connect screen, the About card and the app chrome cannot drift
 * apart on what Inari says it is or where its source lives.
 */

/** One-line description, aimed at someone who has not used Inari before. */
export const PRODUCT_TAGLINE = "Manage S3-compatible object storage.";

/** Mirrors the `org.opencontainers.image.source` label on the runtime image. */
export const SOURCE_URL = "https://github.com/maple52046/inari";
